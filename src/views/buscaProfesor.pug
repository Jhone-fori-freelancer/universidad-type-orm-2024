
extends index

block content
  h1 #{pagina}

  if id
    p ID del Profesor a buscar: #{id}
    
  
    form(method="post", action=`/buscaProfesor/${id}`)
      button.btn(type="submit") Buscar Profesor

  else
    p No se ha proporcionado un ID.
    
  //- Formulario de búsqueda por ID
  form(method="get", action="/buscaProfesor")
    label(for="id") Ingrese el ID del Profesor:
    input(type="text", name="id", placeholder="ID del Profesor")
    button.btn(type="submit") Buscar