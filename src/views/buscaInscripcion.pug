
extends index

block content
  h1 #{pagina}

  if id
    p ID del Inscripcion a buscar: #{id}
    
  
    form(method="post", action=`/buscaInscripcion /${id}`)
      button.btn(type="submit") Buscar Inscripcion

  else
    p No se ha proporcionado un ID.
    
  //- Formulario de búsqueda por ID
  form(method="get", action="/buscaInscripcion ")
    label(for="id") Ingrese el ID del Inscripcion :
    input(type="text", name="id", placeholder="ID del Inscripcion")
    button.btn(type="submit") Buscar